# Tennis Shot Classifier Requirements
# 
# IMPORTANT: Some dependencies must be installed via conda, not pip:
# - conda install pytorch torchvision torchaudio pytorch-cuda=12.9 -c pytorch -c nvidia (or cpuonly)
# - conda install opencv -c conda-forge
# - conda install ffmpeg -c conda-forge -y
#
# Use the automated setup script: setup_environment.bat
# Or follow the manual setup steps in README.md

# Core ML and Data Science Libraries
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0
numpy>=1.21.0
scikit-learn>=1.0.0

# Computer Vision and Image Processing
opencv-contrib-python>=4.5.0
# NOTE: For best compatibility, install OpenCV via conda: conda install opencv -c conda-forge
ultralytics>=8.0.0

# Audio Processing
librosa>=0.9.0
soundfile>=0.10.0

# Video Processing
ffmpeg-python>=0.2.0
# NOTE: The ffmpeg binary must be installed via conda: conda install ffmpeg -c conda-forge -y
# ffmpeg-python is just the Python wrapper, not the actual ffmpeg executable

# Video Downloading (command-line tool)
yt-dlp>=2023.1.6

# Pose Estimation and Motion Analysis
sports2d>=0.8.0
# IMPORTANT: Import as "Sports2D" not "sports2d" in Python code
# Sports2D dependencies for full functionality
onnx>=1.12.0
onnxruntime>=1.13.0
# For GPU acceleration (optional but recommended)
# onnxruntime-gpu>=1.13.0

# Additional Sports2D dependencies
toml>=0.10.0
rich>=10.0.0
tqdm>=4.62.0

# Visualization
matplotlib>=3.5.0
scipy>=1.7.0

# Data Handling
pandas>=1.3.0

# Utilities
Pillow>=8.0.0

# Additional dependencies that might be needed
seaborn>=0.11.0
jupyter>=1.0.0
ipykernel>=6.0.0